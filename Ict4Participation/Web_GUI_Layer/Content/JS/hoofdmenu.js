// Script met de functionaliteiten voor het planning element
$(function () {

    //$('#planning td').click(function (event) {

    //    var $ele = $(this);
    //    var beschikbaar = 'true';

    //    // Check of de tablecell de klasse beschikbaar heeft
    //    if ($($ele).hasClass('beschikbaar')) {
    //        beschikbaar = 'false';
    //    }

    //    // Asynchrone request naar de server
    //    $.ajax({
    //        type: 'POST',
    //        url: 'hoofdmenu.aspx/UpdateBeschikbaarheid',
    //        data: '{day: "' + $(this).data("day") + ', daytime: "' + $(this).data("daytime") + ', beschikbaar: "' + beschikbaar + '"}',
    //        contentType: 'application/json;chatset=utf-8',
    //        dataType: 'json',
    //        success: function (result) {
    //            // Zet de cell actief/niet actief gebaseerd op result.d
    //            //if (result.d == "true") {
    //            //    $(this).addClass("beschikbaar");
    //            //} else {
    //            //    $(this).removeClass("beschikbaar");
    //            //}
    //        }
    //    });

	//});

});